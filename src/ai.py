import heapq


def a_star_search(start, goal, game_map):
    # A*探索アルゴリズムの実装
    # start: 開始位置 (x, y)
    # goal: 目標位置 (x, y)
    # game_map: ゲームマップ
    # ...
    # 最短経路を返す
    return path
